# =============================================================================
# roles/vector-role/handlers/main.yml — Обработчики событий для Vector
# =============================================================================
---
- name: restart vector
  ansible.builtin.systemd:
    name: vector
    state: restarted
    daemon_reload: true